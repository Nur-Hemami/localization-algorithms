Getters and setters for intialPose_ and initialCovariance_ and append use them in EKF_localization function.     DONE
Fix the receiving of the map. On the callback function works fine, but this happens after the call of to EKF_localization function. Maybe I have to wait until the callback is done. Maybe copying the map takes long. DONE
Check if motion model is properly defined. DONE (every motion should end with a stop command)

Create different param.yaml files depending on the starting position.
Develop EFK function.
Create a custom message for the initial pose. No need of the huge covariance matrix.





Packages needed:
Eigen3 -> sudo apt-get install libeigen3-dev